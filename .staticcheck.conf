# staticcheck configuration
# SA1019: deprecated ebiten v2 APIs — suppress until all API calls are updated to v2.9+ equivalents
# U1000:  unused exported/unexported identifiers — large-scope cleanup tracked separately (see AUDIT.md)
checks = ["all", "-SA1019", "-U1000"]
